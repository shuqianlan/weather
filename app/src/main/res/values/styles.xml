<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="VerticalSeekBarStyle" parent="Widget.AppCompat.SeekBar">
        <item name="android:progressDrawable">@drawable/drawable_vertical_seekbar</item>
        <item name="android:thumb">@drawable/drawable_seekbar_thumb</item>
    </style>

    <style name="ProgressBarStyle" parent="Widget.AppCompat.ProgressBar.Horizontal">
        <item name="android:indeterminateDrawable">@drawable/loading_dialogfragment_rotation</item>
        <item name="android:indeterminateOnly">true</item>
        <item name="android:indeterminateBehavior">repeat</item>
        <!--<item name="android:fromDegrees">0</item>-->
        <!--<item name="android:toDegrees">360</item>-->

    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="popupRegionAnimation" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/pop_up</item>
        <item name="android:windowExitAnimation">@anim/pop_down</item>
    </style>

    <declare-styleable name="CarouselLayout" >
        <attr name="duration" format="integer" />
        <attr name="start_delay" format="integer" />
        <attr name="auto_play" format="boolean" />
        <attr name="enable_touch" format="boolean" />
    </declare-styleable>

    <style name="toolbar_popup_theme" parent="Theme.AppCompat.Light">

    </style>

</resources>
